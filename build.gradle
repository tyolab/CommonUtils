apply plugin: 'java'

dependencies {
    compile files('libs/json-java-android.jar')
}

repositories {
    flatDir {
        dirs 'libs'
    }
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"